// @generated by swarm-engine idl v1.0.0
// world hash: 0b4b0be62e3535e1b7b6ef1467249cd53eb0557e5e0a08b585abecb753edb625
// engine version: 0.1.0
// DO NOT EDIT — this file is auto-generated.
export function addCost(target, source) {
    for (const [res, amt] of Object.entries(source)) {
        target[res] = (target[res] ?? 0) + amt;
    }
    return target;
}
export function command(sequence, idempotency_key, action, client_trace_id) {
    return client_trace_id === undefined ? { sequence, idempotency_key, action } : { sequence, idempotency_key, client_trace_id, action };
}
export const actions = Object.freeze({
    move: (object_id, direction) => ({ type: "Move", object_id, direction }),
    harvest: (object_id, target_id, resource) => ({ type: "Harvest", object_id, target_id, resource }),
    transfer: (object_id, target_id, resource, amount) => ({ type: "Transfer", object_id, target_id, resource, amount }),
    withdraw: (object_id, target_id, resource, amount) => ({ type: "Withdraw", object_id, target_id, resource, amount }),
    claimController: (object_id, target_id) => ({ type: "ClaimController", object_id, target_id }),
    spawn: (object_id, spawn_id, body_parts) => ({ type: "Spawn", object_id, spawn_id, body_parts }),
    recycle: (object_id) => ({ type: "Recycle", object_id }),
    build: (object_id, x, y, structure) => ({ type: "Build", object_id, x, y, structure }),
    repair: (object_id, target_id) => ({ type: "Repair", object_id, target_id }),
    upgradeController: (object_id, target_id) => ({ type: "UpgradeController", object_id, target_id }),
    transferToGlobal: (resource, amount) => ({ type: "TransferToGlobal", resource, amount }),
    transferFromGlobal: (resource, amount) => ({ type: "TransferFromGlobal", resource, amount }),
    alliedTransfer: (target_player, resource, amount) => ({ type: "AlliedTransfer", target_player, resource, amount }),
    attack: (object_id, target_id, payload = {}) => ({ type: "Attack", object_id, target_id, ...payload }),
    rangedAttack: (object_id, target_id, payload = {}) => ({ type: "RangedAttack", object_id, target_id, ...payload }),
    heal: (object_id, target_id, payload = {}) => ({ type: "Heal", object_id, target_id, ...payload }),
    hack: (object_id, target_id, payload = {}) => ({ type: "Hack", object_id, target_id, ...payload }),
    drain: (object_id, target_id, payload = {}) => ({ type: "Drain", object_id, target_id, ...payload }),
    overload: (object_id, target_id, payload = {}) => ({ type: "Overload", object_id, target_id, ...payload }),
    debilitate: (object_id, target_id, payload = {}) => ({ type: "Debilitate", object_id, target_id, ...payload }),
    disrupt: (object_id, target_id, payload = {}) => ({ type: "Disrupt", object_id, target_id, ...payload }),
    fortify: (object_id, target_id, payload = {}) => ({ type: "Fortify", object_id, target_id, ...payload }),
    leech: (object_id, target_id, payload = {}) => ({ type: "Leech", object_id, target_id, ...payload }),
    fabricate: (object_id, target_id, payload = {}) => ({ type: "Fabricate", object_id, target_id, ...payload }),
});
export const BODY_PART_COST = {
    Attack: { Energy: 80 },
    Carry: { Energy: 50 },
    Claim: { Energy: 600 },
    Heal: { Energy: 250 },
    Move: { Energy: 50 },
    RangedAttack: { Energy: 100 },
    Tough: { Energy: 10 },
    Work: { Energy: 100 }
};
export function bodyCost(body) {
    const total = {};
    for (const part of body) {
        const cost = BODY_PART_COST[part] ?? {};
        for (const [res, amt] of Object.entries(cost)) {
            total[res] = (total[res] ?? 0) + amt;
        }
    }
    return total;
}
// ── Constants ──
export const SDK_VERSION = "0.1.0";
export const IDL_VERSION = "1.0.0";
export const ABI_VERSION = 2;
export const AI_GAME_DATA_START = "___AI_GAME_DATA_START___";
export const AI_GAME_DATA_END = "___AI_GAME_DATA_END___";
export const MAX_BODY_PARTS = 50;
export const MAX_COMMANDS_PER_PLAYER = 100;
export const MAX_DRONES_PER_PLAYER = 500;
export const MAX_FUEL = 10000000;
export const MAX_JSON_DEPTH = 10;
export const MAX_NEXT_TICK_FUEL_BUDGET = 11000000;
export const MAX_RANGED_ATTACK_RANGE = 3;
export const MAX_REFUND_PER_TICK = 1000000;
export const MAX_TICK_OUTPUT_BYTES = 262144;
//# sourceMappingURL=commands.js.map