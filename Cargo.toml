[package]
name = "swarm-engine"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.15"
indexmap = { version = "2", features = ["serde"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
